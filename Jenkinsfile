pipeline {
    agent any
     
    stages {
        stage("DEV ...") {
            steps {
               sh 'echo DEV' +env.BRANCH_NAME
            }
        }
        stage("Satging ...") {
            steps {
                sh 'echo staging'
            }
        }
        
        
    }   
}
